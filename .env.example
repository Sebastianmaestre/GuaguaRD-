# Puerto donde escucha el servidor TCP GT06 (el que le vas a pasar
# al proveedor / Michael para que configure el dispositivo)
GT06_PORT=5023

# Conexión a Redis
REDIS_URL=redis://localhost:6379

# Conexión a Postgres (Prisma la toma de acá)
DATABASE_URL=postgresql://user:password@localhost:5432/guaguard
